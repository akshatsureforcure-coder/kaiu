<section class="section generic-page page-width">
  <h1 class="generic-page__title reveal">{{ page.title }}</h1>
  <div class="generic-page__content reveal">
    {{ page.content }}
  </div>
</section>

{% schema %}
{
  "name": "Page content"
}
{% endschema %}
